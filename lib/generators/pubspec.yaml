name: generators
description: Generates models for the framework
version: 1.0.0
publish_to: none
environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  source_gen: ^1.2.1
  build: ^2.2.1

dev_dependencies:
  build_runner: ^2.1.8
  flutter_lints: ^1.0.0